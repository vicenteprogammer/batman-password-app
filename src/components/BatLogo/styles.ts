import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageStyle:{
    width: 265,
    height: 150,
    resizeMode: 'contain'
  },
  textStyle:{
    fontSize: 30,
    marginBottom: 15,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#F4CC48'
  }
});