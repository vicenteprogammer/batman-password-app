import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input:{
    width: '100%',
    backgroundColor: '#F4CC48',
    color: 'black',
    fontSize: 20,
    textAlign: 'center',
    borderWidth: 1,
    borderRadius: 5
  }
});